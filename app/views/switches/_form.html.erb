<%= horizontal_form_for @switch do |switch| %>  
  <%= switch.input :name %>  
  <%= switch.input :ip %>  
  <%= switch.input :community %>  
  <%= switch.button :submit, :class => 'btn' %>  
<% end %>
