<h2>Switches</h2>

<ul>
  <% @switches.each do |switch| %>  
    <li><%= link_to switch.name, edit_switch_path(switch) %>  (<%= switch.ip %>) </li>
  <% end %>
</ul>

<%= link_to "new switch", new_switch_path, :class => 'btn' %>  
